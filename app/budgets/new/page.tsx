import Link from "next/link"; import { Suspense } from "react"; import { AppShell, FinancePageLoading } from "@/components/finance/app-shell"; import { BudgetForm } from "@/components/finance/budget-form"; import { PageHeader } from "@/components/finance/finance-ui"; import { Button } from "@/components/ui/button"; import { getActiveWorkspace } from "@/lib/finance/context"; import { getCategories } from "@/lib/finance/data"; import { resolveDashboardPeriod } from "@/lib/finance/dashboard";
type Props = { searchParams: Promise<{ category?: string; month?: string | string[] }> };
async function Content({ searchParams }: Props) { const [workspace, query] = await Promise.all([getActiveWorkspace(), searchParams]); if (!workspace) return <main className="p-6">Nenhum workspace disponível.</main>; const [categories] = await Promise.all([getCategories(workspace.id)]); const period = resolveDashboardPeriod(query.month); return <main className="mx-auto max-w-4xl space-y-6 p-4 sm:p-6 lg:p-8"><PageHeader title="Novo orçamento" description="Defina um limite mensal para uma categoria." action={<Button asChild variant="outline"><Link href={`/budgets?month=${period.month}`}>Voltar</Link></Button>} /><BudgetForm categories={categories} month={period.month} /></main>; }
export default function Page(props: Props) { return <AppShell><Suspense fallback={<FinancePageLoading />}><Content {...props} /></Suspense></AppShell>; }
