import Link from "next/link"; import { notFound } from "next/navigation"; import { Suspense } from "react";
import { AppShell, FinancePageLoading } from "@/components/finance/app-shell"; import { DeactivateRecurrenceButton } from "@/components/finance/deactivate-recurrence-button"; import { MoneyValue, PageHeader, StatusBadge } from "@/components/finance/finance-ui"; import { Button } from "@/components/ui/button"; import { getActiveWorkspace } from "@/lib/finance/context"; import { getRecurrenceOccurrences, getRecurrenceRule } from "@/lib/finance/recurrences/data"; import { formatDate } from "@/lib/finance/format";
type Props = { params: Promise<{ id: string }> };
async function Content({ params }: Props) { const [workspace, route] = await Promise.all([getActiveWorkspace(), params]); if (!workspace) return <main className="p-6">Nenhum workspace disponível.</main>; const [rule, occurrences] = await Promise.all([getRecurrenceRule(workspace.id, route.id), getRecurrenceOccurrences(workspace.id, route.id)]); if (!rule) notFound(); return <main className="mx-auto max-w-5xl space-y-6 p-4 sm:p-6 lg:p-8"><PageHeader title={rule.name} description="A regra gera movimentações independentes." action={<div className="flex gap-2"><Button asChild variant="outline"><Link href="/recurrences">Voltar</Link></Button>{rule.active && <Button asChild><Link href={`/recurrences/${rule.id}/edit`}>Editar</Link></Button>}</div>} /><section className="rounded-2xl border bg-card p-5 shadow-sm"><div className="flex justify-between"><MoneyValue className="text-3xl font-bold" tone={rule.transaction_type === "income" ? "income" : "expense"} value={rule.amount} /><StatusBadge status={rule.active ? "active" : "inactive"} /></div><dl className="mt-6 grid gap-4 sm:grid-cols-3"><div><dt className="text-xs text-muted-foreground">Tipo do valor</dt><dd>{rule.amount_type === "fixed" ? "Fixo" : "Estimado"}</dd></div><div><dt className="text-xs text-muted-foreground">Dia</dt><dd>Todo dia {rule.day_of_month}</dd></div><div><dt className="text-xs text-muted-foreground">Conta</dt><dd>{rule.account?.name}</dd></div><div><dt className="text-xs text-muted-foreground">Início</dt><dd>{formatDate(rule.start_date)}</dd></div><div><dt className="text-xs text-muted-foreground">Fim</dt><dd>{rule.end_date ? formatDate(rule.end_date) : "Sem data final"}</dd></div><div><dt className="text-xs text-muted-foreground">Categoria</dt><dd>{rule.category?.name ?? "Sem categoria"}</dd></div></dl>{rule.active && <div className="mt-6 border-t pt-5"><DeactivateRecurrenceButton id={rule.id} /></div>}</section><section className="space-y-3"><h2 className="text-xl font-semibold">Ocorrências materializadas</h2><div className="divide-y overflow-hidden rounded-2xl border bg-card">{occurrences.map((item) => <Link href={`/transactions/${item.id}`} key={item.id} className="flex items-center justify-between gap-4 p-4 hover:bg-muted/50"><div><p className="font-medium">{formatDate(item.transaction_date)}</p><StatusBadge status={item.status} /></div><MoneyValue tone={item.transaction_type === "income" ? "income" : "expense"} value={item.amount} /></Link>)}</div><p className="text-sm text-muted-foreground">Editar a regra não altera estas movimentações já criadas.</p></section></main>; }
export default function Page(props: Props) { return <AppShell><Suspense fallback={<FinancePageLoading />}><Content {...props} /></Suspense></AppShell>; }
