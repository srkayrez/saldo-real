"use client";
import { useActionState } from "react"; import { createWorkspaceInvitation } from "@/actions/workspaces"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label";
export function WorkspaceInviteForm() { const [state, action, pending] = useActionState(createWorkspaceInvitation, {}); return <form action={action} className="grid gap-4 rounded-2xl border bg-card p-5 shadow-sm sm:grid-cols-2"><div className="space-y-2"><Label htmlFor="invite-email">Email</Label><Input id="invite-email" name="email" type="email" required /></div><div className="space-y-2"><Label htmlFor="invite-role">Permissão</Label><select id="invite-role" name="role" className="h-11 w-full rounded-lg border bg-card px-3 text-sm"><option value="editor">Editor</option><option value="viewer">Visualizador</option></select></div><div className="space-y-3 sm:col-span-2"><Button disabled={pending}>{pending ? "Gerando..." : "Gerar convite"}</Button>{state.error && <p className="text-sm text-destructive">{state.error}</p>}{state.success && <p className="text-sm text-finance-income">{state.success}</p>}{state.inviteUrl && <div><Label htmlFor="invite-url">Link para compartilhar</Label><Input id="invite-url" className="mt-2" readOnly value={state.inviteUrl} /></div>}</div></form>; }
