"use client";
import { useActionState } from "react"; import { renameWorkspace } from "@/actions/workspaces"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label";
export function WorkspaceRenameForm({ name }: { name: string }) { const [state, action, pending] = useActionState(renameWorkspace, {}); return <form action={action} className="flex flex-col gap-3 rounded-2xl border bg-card p-5 shadow-sm sm:flex-row sm:items-end"><div className="flex-1 space-y-2"><Label htmlFor="rename-workspace">Nome do espaço</Label><Input id="rename-workspace" name="name" defaultValue={name} maxLength={120} required /></div><Button disabled={pending}>{pending ? "Salvando..." : "Alterar nome"}</Button>{state.error && <p className="text-sm text-destructive">{state.error}</p>}</form>; }
