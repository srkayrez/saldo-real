"use client";
import { useActionState } from "react"; import { createWorkspace } from "@/actions/workspaces"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { Label } from "@/components/ui/label";
export function WorkspaceCreateForm() { const [state, action, pending] = useActionState(createWorkspace, {}); return <form action={action} className="space-y-4 rounded-2xl border bg-card p-5 shadow-sm"><div className="space-y-2"><Label htmlFor="workspace-name">Nome</Label><Input id="workspace-name" name="name" maxLength={120} placeholder="Ex.: Família Silva" required /></div><Button disabled={pending}>{pending ? "Criando..." : "Criar espaço compartilhado"}</Button>{state.error && <p className="text-sm text-destructive">{state.error}</p>}{state.success && <p className="text-sm text-finance-income">{state.success}</p>}</form>; }
