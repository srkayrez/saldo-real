import { ChevronLeft, ChevronRight } from "lucide-react"; import Link from "next/link"; import { Button } from "@/components/ui/button"; import type { DashboardPeriod } from "@/types/finance";
export function BudgetMonthSelector({ period }: { period: DashboardPeriod }) { return <div className="flex items-center gap-2" aria-label="Mês do orçamento"><Button asChild size="icon" variant="outline"><Link href={`/budgets?month=${period.previousMonth}`} aria-label="Mês anterior"><ChevronLeft /></Link></Button><div className="min-w-36 rounded-md border bg-background px-3 py-2 text-center text-sm font-medium capitalize">{period.label}</div><Button asChild size="icon" variant="outline"><Link href={`/budgets?month=${period.nextMonth}`} aria-label="Próximo mês"><ChevronRight /></Link></Button></div>; }
